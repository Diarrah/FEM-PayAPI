import React from 'react';
import { DemoForm } from '../';

const CTA = () => (
    <div className="cta">
        <h3 className="cta__heading">Ready to start?</h3>
        <DemoForm />
    </div>
)

export default CTA